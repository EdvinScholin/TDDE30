package se.liu.password_manager;

/**
 * This enum class states all constants of buttons that is created in the visual layer.
 */
public enum ButtonOption
{
    ADD, REMOVE, EDIT, LOGIN, QUIT, CONTINUE
}
